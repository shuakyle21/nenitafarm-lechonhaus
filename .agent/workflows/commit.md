---
description: autocommit changes with generated message
---

// turbo-all

1. Stage all changes
```bash
git add .
```

2. Show cached diff for verification
```bash
git diff --cached
```

3. Commit with autogenerated message
```bash
git commit -m "<autogenerate_commit_message>"
```
